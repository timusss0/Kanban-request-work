{
  "name": "board-request",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node local-server.js",
    "local": "node --env-file-if-exists=.env local-server.js",
    "dev": "echo \"no build step\"",
    "build": "echo \"no build step\""
  },
  "dependencies": {
    "mysql2": "^3.24.4"
  }
}